export default function AmbienceCard({ title, description }) {
    return (
        <div className="ambience-card">
            <h4>{title}</h4>
            <p>{description}</p>
        </div>
    );
}
